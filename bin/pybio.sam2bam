# convert sam to bam and sort bam file by position
for fname in $@
do
  samtools view -@ 4 -F 4 -bS ${fname} -o ${fname:0:-4}.bam # -F 4 means only mapped reads
  samtools sort -@ 4 -o ${fname:0:-4}.bam ${fname:0:-4}.bam
  samtools index -b ${fname:0:-4}.bam
done
