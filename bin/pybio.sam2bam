# convert sam to bam and sort bam file by position
for fname in $@
do
  samtools view -F 4 -bS ${fname} > ${fname:0:-4}.bam # -F 4 means only mapped reads
  samtools sort ${fname:0:-4}.bam ${fname:0:-4}
  samtools index ${fname:0:-4}.bam
done
